<% PAGE = {} %>
<% PAGE.Path = "changePassword" %>
<% PAGE.Title = "Промяна на парола" %>
<% PAGE.Description = "" %>
<% PAGE.Keywords = "" %>
<% PAGE.AdditionalCSS = ["forms"] %>
<% PAGE.NoJS = true %>
<html>

    <% include partials/head %>

    <body>

        <% include partials/nav %>

        <div id="Container">

            <form action="" method="post">

                <input value="<%if(typeof userId != "undefined"){%><%=userId%><%}%>" name="idCookie" class="d-none">

                <label>Email
                    <input type="email" name="Email" required>
                </label>

                <label>Нова парола
                    <input type="password" name="Password" required>
                </label>

                <%if(typeof ChangeError != "undefined"){%>
                    <script>
                        swal({
                            title: "<%=ChangeError%>",
                            type: "error"
                        });
                    </script>
                <%}%>

                <%if(typeof Success != "undefined"){%>
                    <script>
                        swal({
                            title: "Успешна промяна",
                            type: "success",
                            footer: "<a href='/login'>Вход</a>"
                        });
                    </script>
                <%}%>

                <input type="submit" value="Промени" class="w-100 mb-3">

            </form>

        </div>

        <% include partials/footer %>

    </body>

</html>
